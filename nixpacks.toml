# Railway 用ビルド設定（Nixpacks）
# デフォルトの PHP ビルドに Node を追加して Vite ビルドを行う
[phases.setup]
nixPkgs = ["...", "nodejs_20"]

[phases.install]
cmds = ["...", "npm ci"]

[phases.build]
cmds = ["npm run build", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]
